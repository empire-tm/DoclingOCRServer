- ## Задача
Нужно разработать FastAPI сервер с Docling и Tesseract OCR.

### Идея проекта
Есть множество документов (doc(x), xls(x), pdf, jpg, png), которые нужно превратить в Markdown с картинками, чтобы потом загружать в другие сервисы, например, RAG-системы.

### Требования
- Язык OCR: русский и английский;
- Так же использовать OCR распознавания таблиц и layout документов, который есть в docling;
- Деплой через Docker;
- Поддержка GitHub Actions - для того чтобы можно было публиковать пакеты для Docker.

### Процесс
1) Вызывается метод POST: documents/process Загружается файл.
2) Прогоняется через docling и создаются файлы и папки во временном хранилище:
📁 {task_id}
    📄 document.md
    📁 images:
        📷 image_1.jpg
        📷 image_2.png
        ...
        📷 image_{n}.jpg
3) Файлы упаковываются в {task_id}.zip
4) Возращется zip файл клиенту